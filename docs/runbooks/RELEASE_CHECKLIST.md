# Release Checklist

- [ ] `/login` renders.
- [ ] Root route redirects to `/login`.
- [ ] `/dashboard` protected route works.
- [ ] Auth callback route exists.
- [ ] Backend reachable via `NEXT_PUBLIC_API_BASE_URL` (`https://ngoinfo-grantpilot-production.up.railway.app`).
- [ ] No hardcoded localhost URLs in frontend code.
- [ ] Logo loaded from canonical URL per `BRAND_AND_FRONTEND_SPEC.md`.
